<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Chi tiết đơn hàng</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-4">
        <h2 class="mb-4">📦 Chi tiết đơn hàng</h2>

        <div class="card">
            <div class="card-header">
                Mã đơn hàng: <strong><%= order._id %></strong>
            </div>
            <div class="card-body">
                <h5 class="card-title">Thông tin khách hàng</h5>
                <p class="card-text">Họ và tên: <%= order.customerName %></p>
                <p class="card-text">Địa chỉ: <%= order.customerAddress %></p>
                <p class="card-text">Số điện thoại: <%= order.customerPhone %></p>

                <h5 class="card-title">Sản phẩm</h5>
                <table class="table table-bordered">
                    <thead class="table-dark">
                        <tr>
                            <th>Sản phẩm</th>
                            <th>Giá</th>
                            <th>Số lượng</th>
                            <th>Tổng</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% order.items.forEach(item => { %>
                            <tr>
                                <td><%= item.name %></td>
                                <td><%= item.price.toLocaleString() %>₫</td>
                                <td><%= item.quantity %></td>
                                <td><%= (item.price * item.quantity).toLocaleString() %>₫</td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>

                <h5 class="card-title">Tổng cộng: <strong><%= order.totalAmount.toLocaleString() %>₫</strong></h5>
                <p class="card-text">Trạng thái: <%= order.status %></p>
            </div>
        </div>

        <a href="/orders" class="btn btn-outline-primary mt-4">Quay lại danh sách đơn hàng</a>
    </div>
</body>
</html>